#!/usr/bin/env fish

if test -z "$argv[1]"
    echo "Error: A file name must be set, e.g. on \"the wonderful thing about tiggers\"."
    exit 1
end

set dir_name (echo $argv[1] | tr ' ' '-')

mkdir -p $dir_name
touch $dir_name/__init__.py
